---
const navItems = [
  { name: "Home", href: "/" },
  { name: "Products", href: "/coffees" },
  { name: "About", href: "/#about" },
  { name: "Contact", href: "/#contact" },
];
---

<nav class="bg-white shadow-md fixed w-full z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <div class="flex items-center">
        <a href="/" class="text-2xl font-bold text-coffee-dark">CaféDelight</a>
      </div>

      <!-- Mobile Menu Button -->
      <div class="md:hidden flex items-center">
        <button
          id="menu-toggle"
          class="text-gray-700 focus:outline-none focus:text-coffee-light"
        >
          <svg
            class="h-6 w-6"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16m-7 6h7"></path>
          </svg>
        </button>
      </div>

      <!-- Desktop Menu -->
      <div class="hidden md:flex items-center space-x-8">
        {
          navItems.map((item) => (
            <a
              href={item.href}
              class="text-gray-700 hover:text-coffee-light transition-colors"
            >
              {item.name}
            </a>
          ))
        }
        <a
          href="/login"
          class="bg-coffee-light text-white px-4 py-2 rounded-md hover:bg-coffee-dark transition-colors"
        >
          Login
        </a>
      </div>
    </div>
  </div>

  <!-- Mobile Menu Dropdown -->
  <div
    id="mobile-menu"
    class="hidden md:hidden absolute top-16 left-0 w-full bg-white shadow-md py-4 flex flex-col items-center space-y-4"
  >
    {
      navItems.map((item) => (
        <a
          href={item.href}
          class="text-gray-700 hover:text-coffee-light transition-colors"
        >
          {item.name}
        </a>
      ))
    }
    <a
      href="/login"
      class="bg-coffee-light text-white px-4 py-2 rounded-md hover:bg-coffee-dark transition-colors"
    >
      Login
    </a>
  </div>
</nav>